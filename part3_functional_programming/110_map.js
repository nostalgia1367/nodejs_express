// define
const numArray = [1, 2, 3];

// process
const multipledList = numArray.map(item => item * 10);


console.log(typeof(multipledList));
console.log(multipledList);


multipledList.forEach(it => console.log(it));
